struct Point { bool valid; int[][] coords; }
enum Kind { A; B = 2; }

void main() {
	struct Point p = { .valid = true, .coords = [[1, 2], [3, 4]] };
	print(p.valid);
	enum Kind k = (enum Kind)B;
	int v = k.B;
	print(v);
}
