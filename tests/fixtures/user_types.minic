struct Point { bool valid; int[][] coords; }
enum Kind { int B; A; }

void main() {
	struct Point p = { .valid = true, .coords = [[1, 2], [3, 4]] };
	print(p.valid);
	enum Kind k = { .B = 42 };
	match k {
		case B: { print(B); }
		case A: { print(0); }
	}
}
