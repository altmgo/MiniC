struct Point { bool valid; int x; }
enum Kind { int B; A; }

void main() {
	struct Point p = { .valid = true, .x = 42 };
	print(p.valid);
	enum Kind k = { .B = 42 };
	match k {
		case B: { print(B); }
		case A: { print(0); }
	}
}
